import 'package:flutter/material.dart';

abstract class AppColors {
  // Cores de Marca
  static const Color acaiPurple = Color(0xFF4B0082); // Roxo Profundo
  static const Color guaranaRed = Color(0xFFD32F2F); // Vermelho Intenso
  static const Color neonGreen = Color(0xFF39FF14);  // O "Speed" Neon
  
  // Cores de Fundo (Dark Mode)
  static const Color backgroundBlack = Color(0xFF121212); // Fundo Absoluto
  static const Color surfaceDark = Color(0xFF1E1E1E);     // Cartões/Superfícies
  
  // Cores Funcionais
  static const Color glassWhite = Color(0x1AFFFFFF); // Branco 10% Opacidade (Vidro)
  static const Color textPrimary = Color(0xFFEDEDED);
  static const Color textSecondary = Color(0xFFB0B0B0);
}